import { ScrollToInvalidDirective } from './scroll-to-invalid.directive';

describe('ScrollToInvalidDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollToInvalidDirective();
    expect(directive).toBeTruthy();
  });
});
